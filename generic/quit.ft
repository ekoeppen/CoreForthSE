DECIMAL

: INTERPRET
    TIB (SOURCE) !   0 >IN !
    ACCEPT   SOURCE# !   SPACE
    (INTERPRET) IF DROP S"  ok " TYPE ELSE COUNT TYPE 63 EMIT THEN CR ;

: QUIT   0 STATE ! BEGIN INTERPRET AGAIN ;
: ABORT   S" CoreForth revision NNNNNNNN ready." TYPE CR   QUIT ;
: ?ABORT   ROT IF TYPE ABORT THEN 2DROP ;
: TURNKEY HEX ABORT ;

